/**
 * @file serial_commands.cpp
 * @brief Backward-compatibility stub — implementation is now in commands.cpp.
 */

// Nothing to define here: all symbols are in commands.cpp (namespace commands).
// This file is kept so that any build rules referencing it remain valid.
